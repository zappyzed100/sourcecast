# Record of upstream source selection and how each one is integrated/updated.
# When adopting a new upstream (or new skill paths), add it here and keep
# scripts/setup-upstreams.ps1 and .github/workflows/update-upstreams.yml in sync.
# All upstream checks run weekly on the same slot (Mon 06:00 UTC).
# Each entry records the rationale so the selection can be re-evaluated later
# without reconstructing the reasoning (lesson from 2026-07-15: the original
# selection had no recorded rationale).

sources:
  - id: guardrails-kit
    repository: zappyzed100/guardrails-kit
    integration: submodule
    update: dependabot
    scope: repository
    rationale: >-
      The whole repository is the product base; nothing to cherry-pick.

  - id: emil-ui-skills
    repository: emilkowalski/skills
    integration: submodule-sparse-checkout
    paths:
      - skills/animation-vocabulary
      - skills/apple-design
      - skills/emil-design-eng
      - skills/improve-animations
      - skills/review-animations
    update: path-filtered-github-action # update-upstreams.yml, weekly Mon 06:00 UTC
    rationale: >-
      Adopted first among the UI-skill candidates because it is the only one
      whose shape fits submodule + sparse-checkout: small self-contained
      Markdown skills under skills/<name>/, one recognized author
      (design engineer; Vercel/Linear background), no runtime or install
      footprint. 2026-07-15: expanded from 2 skills (emil-design-eng,
      review-animations) to all 5 — user decision: the extra skills are
      Markdown-only, cost nothing to carry, and will help once UI work starts.
      New skills added upstream later remain opt-in (explicit paths, no
      wildcard).

  # --- Not integrated (recorded so candidates and policy are not lost) ---

  - id: awesome-design-md
    repository: voltagent/awesome-design-md
    integration: submodule-sparse-checkout
    paths:
      - README.md
      - LICENSE
      - design-md/**/DESIGN.md
    update: path-filtered-github-action # update-upstreams.yml (watch: design-md/), weekly Mon 06:00 UTC; Dependabot ignores it
    status: reference
    rationale: >-
      Local reference (2026-07-15, revised from remote-link-only the same
      day): the user pointed out that agents actually consult material that
      is local and pointed to from CLAUDE.md, not URLs in a record file.
      74 per-brand DESIGN.md analyses, sparse-checked-out to just the
      DESIGN.md files (~2.3MB working tree; the parent repo stores only a
      gitlink, and submodule contents are invisible to git ls-files so no
      kit-check exclusions are needed). Not Claude skills — consult when
      choosing/refining the UI's visual direction. Updated weekly by
      update-upstreams.yml on the shared Monday slot (user decision
      2026-07-15, revising the earlier manual-only policy), with a
      design-md/ path filter so metadata-only upstream commits don't PR.

  - id: ui-ux-pro-max-skill
    repository: nextlevelbuilder/ui-ux-pro-max-skill
    integration: vendored-copy
    vendored-to: .claude/skills/
    vendored-from-sha: f8ac5e1266dba8354ea96e19994d9f4345e7ec31 # GitHub main, v2.11.0
    update: scripts/revendor_uipro.py # run weekly by update-upstreams.yml (PR only when the vendored copy changed); manual run also fine
    status: adopted
    special-handling:
      # All three ride the kit's fill machinery (fill_bindings.py / managed
      # BINDING sections, preserved across kit upgrades by install_kit.py).
      # Exact fill lines are recorded in CLAUDE.md; summary:
      - python-via-uv: >-
          Fill scripts/dev.py COMMANDS (additive) with a `design` verb that
          runs `uv run python .claude/skills/ui-ux-pro-max/scripts/search.py`
          (kit GUARDRAILS §7.1 — verb rail instead of a read-and-obey rule).
          Smoke-tested via uv on 2026-07-15.
      - vendored-exclusion: >-
          Fill scripts/repo_scan.py BINDING with
          GENERATED_PATTERNS += ^\.claude/skills/ — generated semantics
          (hand-edit forbidden, excluded from content checks and STRUCTURE.md
          index; check_structure.py never reads generated files). gitleaks
          intentionally still applies. Verified against kit source 2026-07-15.
      - generated-artifact: >-
          Fill scripts/repo_scan.py BINDING with
          GENERATED_PATTERNS += ^design-system/ — `--persist` output is
          committed (design decisions) but exempt from checks/index.
      - note: >-
          emil-ui-skills need no handling: upstream/ submodule contents never
          appear in `git ls-files`, so repo_scan cannot see them.
    rationale: >-
      Adopted into the workbench 2026-07-15 (user decision: accept the three
      special handlings above; merging into the guardrails-kit repo itself was
      considered and rejected — different layers, double upstream tracking).
      Verified before adoption (repo inspected at v2.11.0): installs
      self-contained into .claude/skills/ (orchestrator SKILL.md + CSV
      databases + Python search scripts + 6 sub-skills); does NOT touch
      CLAUDE.md, .claude/settings.json, or .mcp.json — no file-level collision
      with guardrails-kit. Scripts make no network calls (offline BM25
      search). NOTE the npm package (ui-ux-pro-max-cli) lags GitHub main, so
      we vendored by reproducing the installer's render+copy from a clone of
      the pinned SHA instead of `npx ui-ux-pro-max-cli init --ai claude`.
      To update: run `uv run --no-project scripts/revendor_uipro.py` — it
      re-renders SKILL.md, replaces the vendored skill dirs, and bumps
      vendored-from-sha in one command (verified byte-identical when the
      upstream SHA is unchanged).
